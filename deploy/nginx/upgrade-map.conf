# Snippet included by main nginx.conf via sites-enabled.
# Connection upgrade map — install script ensures this exists once.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
